node {
    def VENV_DIR = "${env.WORKSPACE}/venv"  // Define the virtual environment directory
    def reportFile = "${env.WORKSPACE}/unit-test-report.txt"

    try {
        stage('Clone Repository') {
            echo 'Cloning the repository...'
            git branch: 'main', url: 'git@github.com:avengers-p11/attendance-api.git'
        }

        stage('Install System Dependencies') {
            echo 'Installing required system dependencies...'
            sh '''
                sudo apt-get update -y
                sudo apt-get install -y python3.11 python3.11-venv python3-pip curl
            '''
        }

        stage('Set Up Python Environment') {
            echo 'Setting up the Python virtual environment...'
            sh """
                if [ ! -d "${VENV_DIR}" ]; then
                    python3.11 -m venv ${VENV_DIR}
                fi
                . ${VENV_DIR}/bin/activate  # Correct the path to the virtual environment's activation script
                pip install --upgrade pip
            """
        }

        stage('Install Poetry') {
            echo 'Installing Poetry...'
            sh """
                . ${VENV_DIR}/bin/activate  # Correct the path to the virtual environment's activation script
                pip install poetry
            """
        }

        stage('Regenerate Poetry Lock File') {
            echo 'Regenerating the poetry.lock file...'
            sh """
                . ${VENV_DIR}/bin/activate  # Correct the path to the virtual environment's activation script
                poetry lock --no-update  # Regenerate the lock file without updating dependencies
            """
        }

        stage('Install Project Dependencies with Poetry') {
            echo 'Installing project dependencies using Poetry...'
            sh """
                . ${VENV_DIR}/bin/activate  # Correct the path to the virtual environment's activation script
                poetry install
            """
        }

        stage('Run Tests') {
            echo 'Running unit tests using pytest...'
            sh """
                . ${VENV_DIR}/bin/activate  # Correct the path to the virtual environment's activation script
                pytest > ${reportFile} || true  # Capture the test results in a report file
            """
        }

    } catch (Exception e) {
        echo "Pipeline failed: ${e.message}"
        mail(
            to: 'it.mohitsaini@gmail.com',
            subject: 'Pipeline Failed',
            body: 'The Jenkins pipeline failed. Please check the logs.'
        )
        throw e
    } finally {
        stage('Cleanup') {
            echo 'Cleaning up...'
            sh "rm -rf ${VENV_DIR}"
            archiveArtifacts artifacts: 'unit-test-report.txt', allowEmptyArchive: true
            echo 'Pipeline completed.'
        }

        stage('Send Email') {
            if (fileExists(reportFile)) {
                if (currentBuild.result == null || currentBuild.result == 'SUCCESS') {
                    emailext(
                        to: 'it.mohitsaini@gmail.com',
                        subject: "Jenkins Build SUCCESS: Job ${env.JOB_NAME}",
                        body: """SUCCESS: Job ${env.JOB_NAME} build ${env.BUILD_NUMBER}
More info at: ${env.BUILD_URL}""",
                        attachmentsPattern: 'unit-test-report.txt',  // Attach the test report
                        attachLog: true
                    )
                } else {
                    emailext(
                        to: 'it.mohitsaini@gmail.com',
                        subject: "Jenkins Pipeline Failure: ${env.JOB_NAME}",
                        body: '''Hello,

The Jenkins pipeline has failed. Please check the logs.'''.stripIndent()
                    )
                }
            } else {
                echo "Error: Attachment file not found: ${reportFile}"
            }
        }
    }
}
